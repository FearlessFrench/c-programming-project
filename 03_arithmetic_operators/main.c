#include <stdio.h>

int main() {

    // arithmetic operators = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z = 0;

    //z = x + y; 5
    //z = x - y; -1
    //z = x * y; 6

    // when dividing we should change to float
    int x = 2;
    float y = 3;
    float z = 0;
    //z = x / y; 0.666667

    
    int x = 10;
    int y = 3;
    int z = 0;
    // modulus operator
    //z = x % y; 1
    
    // augmented operators
    //x--;
    x = x + 2 // x+=2
    x = x - 3 // x-=3
    x = x * 2 // x*=2
    x = x / 2 // x/=2

    printf("%d", z);

    return 0;
}