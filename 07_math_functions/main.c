#include <stdio.h>
#include <math.h> // for declaration like sqrt()

int main()
{
    //int x = 9;

    //x = sqrt(x);
    //x = pow(x, 4);


    float x = 3.14;

    //x = round(x); round to nearest integer
    //x = ceil(x); always round up
    //x = floor(x); always round down


    int x = -3;

    //x = abs(x); |-3| >>> 3


    int x = 3

    x = log(x); 
    x = sin(x);
    x = cos(x);
    x = tan(x);

    printf("%d", x);

    return 0 ;
}